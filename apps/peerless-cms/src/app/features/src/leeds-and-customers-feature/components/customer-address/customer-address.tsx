import { LeedCustomerAddresses } from "../leed-customer-addresses/leed-customer-addresses";

export interface CustomersAddressProps {}

export function CustomersAddress(props: CustomersAddressProps) {
    return (
        <LeedCustomerAddresses />
    )
}