import { CommonProps } from '@peerless/models';
import { FC } from 'react';

import { Login } from '@peerless-cms/components';

export const LoginPage: FC<CommonProps> = () => {
  return <Login />;
};
