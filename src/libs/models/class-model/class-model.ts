export interface ClassPayload{
    name: string;
    description: string;
    grade: string | number;
    count: string;
    address: string;
}

export interface DataArray {
    value: string;
    label: string;
}